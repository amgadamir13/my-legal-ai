# Secrets must be configured in the Streamlit deployment settings or locally.
# Never place API keys, usernames, or passwords in this repository.
# Example local-only settings:
# GEMINI_API_KEY = "..."
# APP_USERNAME = "..."
# APP_PASSWORD_SHA256 = "..."
